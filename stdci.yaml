---
substage:
  - lint:
      script:
        fromfile: test.sh
  - unit-tests:
      script:
        fromfile: 'automation/check-patch.sh'
  - check-docs:
      script:
        fromfile: test.sh
release_branches:
  master: dummy-master
